import { Metadata } from "next";

export const metadata: Metadata = {
  title: "The .cursorrules File | Production-Ready AI Configuration | 200x.dev",
  description: "The exact .cursorrules file used to build dozens of production applications. One-shot Next.js apps and landing pages with zero drift.",
};

export default function CursorrrulesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}


